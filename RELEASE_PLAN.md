# 发布计划

## 当前发布方式

通过 GitHub 分发源码。用户拉取仓库后使用根目录 `./resume` 启动，不提供安装包，也不依赖域名、服务器、数据库或独立账户。

仓库只保留：

- Python 核心控制器与安全策略
- `./resume` 命令行入口
- 固定监听本机 `127.0.0.1` 的 Web 管理界面
- 仓库级 Codex Skill
- 核心、Web 和前端隔离测试
- 用户说明与发布卫生检查

仓库不再包含原生 macOS 客户端、PyInstaller/签名/公证脚本、微信小程序、移动端联调代码、npm 依赖和跨设备控制入口。

## 发布验收

1. 全新拉取后，无第三方依赖运行 `./resume --help`、`./resume doctor` 和 `./resume web`。
2. 从仓库外目录调用 `resume`，确认模块定位不依赖当前工作目录。
3. 运行 Python 核心与 Web 回归测试。
4. 运行浏览器脚本测试和语法检查。
5. 运行 Python 编译和跟踪文件卫生检查。
6. 只在明确授权、隔离且无副作用的测试任务上做真实发送验证。
7. 不用真实业务任务替代测试，不把离线模拟视为自然额度重置验证。

## 尚未完成

真实账号经历完整的额度耗尽、自然重置和自动续跑周期仍未完成端到端验证。此限制必须继续显示在 README 和界面中，不能因单元测试通过而删除。
