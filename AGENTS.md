# 开发边界

本项目是 Codex App 本地续跑实验工具。保持单代理、局部修改、实际验证；不启动正式团队流程。

用户明确要求时可启用并行独立审计；各审计者先只读复现，由主代理分配互不冲突的修复范围，再独立复核。不因此启动正式 SDLC 流程。

- 不绕过账户限额、登录或审批，不使用重置券或付费替代渠道。
- 只管理用户明确选中的本地任务。不能自动扫描并续跑所有未完成任务。
- 官方查询连接只读；App 消息写入仅限经状态核验的原任务续跑。对明确托管的任务，可通过已核验的原对话链接加载页面以恢复连接；不得预填/发送其他提示词、另起线程或接管所有权。历史状态仅用于筛选，发送必须重新核验实时状态。
- 发送结果不确定时禁止自动重发。保持落盘去重、取消和预算检查。
- 不通过模糊匹配“继续”或“额度”等对话文本判断失败原因。
- App 更新需要重新验证协议；不能为了通过测试放宽版本保护。
- 修改后运行 `python3 -m unittest discover -s tests -v`。真实写入测试需要明确授权及无副作用测试任务，不能在用户业务任务中试发消息。
- 区分离线模拟、真实只读验证、真实发送验证、自然额度重置验证，不能将其互相替代。

- 修改前端还需运行 `node --test tests/test_ui.cjs` 和 `node --check codex_resume/static/app.js`；异步检查必须处理过期成功、过期失败与同 UUID 再次选中的竞态。
