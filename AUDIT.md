# 对抗审计与修复记录

日期：2026-08-29。用户明确要求并行审计；暂停域名测试。

## 范围与方法

- 审计基线：`1270401`，标签 `audit-baseline-20260829`。
- 四个独立审计者分别负责核心续跑、Web/API、小程序、App 传输。先只读复现，再分配文件修复，最后交叉复核。
- 仅使用临时数据库、socketpair、假 App、回环 HTTP/TLS、可控前端响应。没有开启/停止真实托管、发送真实任务消息、改账户额度或公开服务。
- 原始会话截图及本机日志不提交；安全扫描只是一组已知模式检查，不代表秘密检测完备。

## 已复现问题

下表问题均已修复并通过隔离回归及交叉复核。P1 为发送/防重边界或服务可用性的重要问题，P2 为状态、隐私或功能正确性问题。

| 编号 | 级别 | 问题与复现条件 | 修复方向 |
|---|---|---|---|
| A01 | P1 | 最后一次 App 快照读取期间用户 stop，快照返回后仍发送；后台回写还能覆盖 paused 描述 | 最终发送前检查对应持久发送意图、enabled 和预算；后台更新不覆盖已停止记录 |
| A02 | P2 | start 已 arm 后日志打开/Popen 失败，留下 enabled=1、starting 的假托管 | 启动失败关闭记录，保留并发用户 stop |
| A03 | P2 | start 释放父锁到子进程取得锁之间，第二个CLI可覆盖预算 | 父到子显式继承同一 flock 描述符，全程不释放监控锁 |
| A04 | P1 | 首个快照之后已有新状态事件排在 socket 中，发送在处理该事件之前发生 | 发送前有界处理已到达事件；发现所选任务状态变化则拒发 |
| A05 | P1 | HTTPS 客户端只建立TCP不发ClientHello，主accept线程卡在握手，合法连接也超时 | 握手移入有超时的worker，并限制并发连接数 |
| A06 | P2 | Web 对错任务/无效状态响应误报成功，未知POST缺少跨刷新防重 | 严格验证确认身份和状态，持久保存未知意图，串行写操作，人工核对后解锁 |
| A07 | P2 | 旧登录请求晚于新登录返回，旧catch/finally断开新连接或污染UI | 按认证代次丢弃过期成功、失败及清理回调 |
| A08 | P2 | 小程序开启前检查期间 hide→show，visible恢复使旧操作仍能发送 | 生命周期函数使操作代次失效；开启、停止确认、解锁均复核代次 |
| A09 | P1 | 小程序旧client迟到响应无条件删除pending，清掉新client的操作锁 | 操作拥有唯一标识，只允许对应响应清理自己的意图 |
| A10 | P2 | 只读比较失败时assert错误包含真实标题/路径，并被直接输出stderr | 比较失败只输出安全字段名称，不输出差异内容或原始错误 |
| A11 | P2 | 只读验证HTTP适配器固定使用http.request，HTTPS地址必定失败 | 按协议选择HTTP/TLS传输，证书信任仅限隔离测试客户端 |
| A12 | P2 | Python 3.9 socket.timeout 不属于内建TimeoutError，暂时断连被当成连续永久错误而停机 | 控制器和资格查询显式处理socket.timeout，恢复前仍不授权发送 |
| A13 | P2 | 小程序匹配UUID但未知status的开启响应仍清除待确认保护 | 仅接受后端已知状态，未知响应保留意图 |
| A14 | P2 | 小程序写操作期间卸载页面，disconnect被writing保护拦住，旧预检查回调仍能发送 | 独立生命周期清理；卸载使代次失效，已发但未确认的意图仍保留 |

## 审计决策

- 确认问题必须有隔离复现或能失败的回归，不将推测或未验收事项直接列为漏洞。
- 核心与传输审计者交叉复核锁继承、启动回滚和最终发送守卫；Web与手机审计者反向复核状态和操作意图。交叉复核发现新Web锁回调等待期间换凭据仍可发送旧任务，已加入锁内代次复核及专门回归。
- 曾报告“微信SDK脚本缺少核心断言”，主代理完整核对基线源码后证明该判断来自读取片段不完整；审计者撤回，不计为缺陷，不改成永远失败的替代脚本。
- 首版内部 App 接口没有原子的 compare-and-start。排空已到达事件和最终本地守卫减少已确认窗口，不能消除最后检查后 App 瞬时变化；不能宣传完全无竞态。
- 历史分页预算在单次只读调用中可能超出总预期，是剩余可用性风险；不会因此授权发送，现有有界超时和失败关闭仍生效。
- 任务持续在线时被归档的所有真实快照表现尚缺协议证据，不猜测新增字段；归档资格筛选已有回归，此情形仍是覆盖缺口。
- npm audit报告0漏洞，但微信自动化SDK的部分开发依赖已弃用；它们不进入Python服务或小程序发布包，不能把0漏洞等同于依赖维护状态良好。
- 手机真机、正式域名与完整自然额度周期验收仍独立待办。本轮不将离线回归或模拟器历史证据替代这些验收。

## 验证与版本

主代理重新执行：Python **98/98**，Node **52/52**（Web26、小程序23、HTTP3），均通过；锁定依赖重装后再次执行Node仍全部通过。Python compileall、8个JavaScript文件语法检查、git diff --check通过。npm audit报告0项漏洞。

新增版本/发布卫生检查对正常仓库、误跟踪私有文件、版本不一致三类场景实测通过；错误输出不回显秘密内容。CI使用固定Action提交、只读权限、15分钟时限，在Ubuntu/macOS运行隔离测试。**远程CI尚未运行**，不能把本地通过等同远程通过。

主代理真实内置浏览器联调（仅独立模拟端口）：首次开启在后端成功但故意回错误UUID，页面显示待确认；刷新和新标签页保留保护，停止按钮被禁止；刷新记录并明确勾选人工核对后解除，两页同步且没有自动POST；随后停止请求成功。后端共收到1次模拟start、1次模拟stop，最终paused。此为真实浏览器持久化/跨标签展示验证，严格同时争锁的竞争场景另由受控锁回归覆盖。

版本提高为 **0.1.1**，基线、核心修复、Web修复、手机修复、版本/CI/报告分别提交。远程目标尚未确认，推送和远程CI不是本轮已完成项。

| 提交 | 内容 |
|---|---|
| `1270401` | 脱敏后的审计基线 |
| `13cd136` | 核心取消/锁交接/启动失败/传输事件/超时修复 |
| `b93731b` | TLS及Web状态与未知操作保护 |
| `2b8249e` | 小程序生命周期、意图归属与只读验证修复 |

版本/CI/本报告由后续提交归档，最终使用本地标签 `v0.1.1` 定位。标签不代表已推送或创建远程Release。
