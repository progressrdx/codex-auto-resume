# 验证摘要

## 0.1.1 对抗审计后 · 2026-08-29

四个独立审计者完成复现、专属范围修复和交叉复核；主代理再次运行Python98项、Node52项，全部通过。锁定依赖重装、语法、编译、发布卫生检查通过。真实浏览器在独立模拟服务中验证错误确认、刷新/第二标签页保护、人工解锁不重发、正常停止及后端一致性。完整问题与测试边界见 [AUDIT.md](AUDIT.md)。

远程仓库地址尚未确认，CI已配置但未远程运行。原有Web/watch进程未重启，不宣称补丁已热应用。下文为审计前历史基线。

## 审计前基线 · 2026-08-29

| 层次 | 已确认结果 | 不代表什么 |
|---|---|---|
| Python 单元与集成 | 87 项通过，含策略、并发、传输、HTTP/TLS边界 | 不是所有真实 App 状态均已覆盖 |
| Node 前端与 HTTP | 34 项通过，含 Web、小程序交互与隔离 HTTP 联调 | 不是手机真机验证 |
| 静态检查 | Python compileall、JavaScript 语法通过；npm audit 0项漏洞 | 不等于逻辑/安全审计无问题 |
| 微信模拟器 | 实际 wx.request 查询、模拟任务开启/停止、停止取消；真实账户只读比对一致 | 不是手机 HTTPS 或发布版域名验证 |
| 真实发送 | 用户授权无副作用测试任务单次发送；另观察到生产控制器一次发送并继续工作 | 完整自然额度失败→等待→恢复的证据链尚未最终验收 |
| 官方微信自动化 SDK | 能连接，但本机页面方法调用超时，未通过 | 不将原生界面人工操作结果冒充 SDK 自动化成功 |

原始截图、真实任务标识和机器路径仅保存在本机被忽略的 `tests/evidence/`，不推送远程。历史详细记录已保留到该目录；本摘要不抹除原始失败。

## 复现命令

```sh
python3 -m unittest discover -s tests -v
python3 -m compileall -q codex_resume
npm ci --ignore-scripts
npm test
node --check codex_resume/static/app.js
npm audit
```

测试使用隔离假任务、临时数据库和回环 HTTP/TLS，不连接真实 App 发消息。真实账户只读验证及微信模拟器原生操作步骤见 `miniprogram/README.md`。未知结果不视为成功，真实任务写入需另行明确授权及无副作用测试任务。

## 暂停与待验证

用户当前暂停域名测试。未建立公网隧道、修改系统信任或公开真实任务服务；未做手机真机验收。对抗审计期间不重新开启/停止生产托管，也不改变次数上限。已知内部协议版本锁定和非原子状态复查限制仍见 README。
