# 版本记录

## 0.3.0 — Windows 预览适配（未发布）

- 新增 Windows 命名管道 IPC、Windows 文件锁和后台 watcher 启动支持。
- 新增 `resume.cmd` 与 `resume.ps1`，支持在 PowerShell 或命令提示符中直接启动。
- 自动定位 Windows Codex App 复制到用户目录的 `codex.exe`，并核验 Store/MSIX App 与 CLI 版本。
- 保留精确版本保护；Windows 自动化路径已隔离测试，真实 Windows 自然额度重置周期仍待验证。

## 0.2.0 — 源码精简版（2026-08-29）

- 发布方式统一为 GitHub 源码拉取和本机手动启动。
- README 增加完整的下载、启动、界面操作、配置、更新和安全边界说明。
- 仓库只保留 Python 核心、CLI、本机 Web UI、Codex Skill 和对应测试。
- 删除原生 macOS 客户端、小程序、移动端联调、打包签名代码及 npm 依赖。
- Web 控制台固定监听本机回环地址，不再提供跨设备入口。
- 真实额度耗尽跨自然重置周期仍待端到端验证。

## 0.1.1 — 核心安全修复（2026-08-29）

- 发送前重新核对取消、预算、发送意图和 App 实时状态。
- 父子进程继承同一监控锁；启动失败不留下假托管。
- Web 增加跨标签写锁、持久待确认意图和过期会话保护。
- 增加版本保护、发布卫生检查和隔离 CI。

## 0.1.0 — 实验基线（2026-08-29）

- 完成本地选定任务续跑控制器、额度查询、状态持久化和 Web 控制台。
